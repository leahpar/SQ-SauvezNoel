{% extends 'base.html.twig' %}

{% block body %}
<div class="example-wrapper">
    <h1>Admin</h1>

    <ul>
        {% for message in messages %}
        <li>
            {{ message.nom }}
            {{ message.message }}
            {{ message.date|date("m/d H:i") }}
        </li>
        {% endfor %}
    </ul>

</div>
{% endblock %}
